table
    tr
      th  Game Name
      th  Terminate
    each game in games
      tr
        if !game.attributes.isFinished
          td= game.attributes.Name
          th
            button(onclick='window.location.href="/checkoutAll/' + game.id + '"', value = "Checkout")